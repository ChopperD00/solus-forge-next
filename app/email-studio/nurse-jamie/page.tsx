import EmailStudioCard from '@/app/components/workflow-cards/EmailStudioCard';

export const metadata = {
  title: 'Email Studio | Nurse Jamie',
  description: 'Generate email campaigns from Google Sheets copy + uploaded assets',
};

export default function NurseJamieEmailStudioPage() {
  return <EmailStudioCard />;
}
